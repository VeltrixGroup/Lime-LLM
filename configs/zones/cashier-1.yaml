# Placeholder zones so configs/example.yaml loads out of the box.
# Draw the real zones for your camera before going live:
#   uv run storeguard draw-zones --source <rtsp-url-or-video> --out configs/zones/cashier-1.yaml
zones:
  - name: register-1
    points: [[0.35, 0.40], [0.65, 0.40], [0.65, 0.70], [0.35, 0.70]]
